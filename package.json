{
  "name": "goodlookingsite",
  "version": "1.0.0",
  "description": "Michael's Portfolio",
  "main": "script.js",
  "scripts": {
    "format": "prettier --write .",
    "format-watch": "onchange './src' -- prettier --write {{changed}}",
    "start": "http-server ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.12.3",
    "onchange": "^7.0.2",
    "prettier": "^2.0.5"
  }
}
